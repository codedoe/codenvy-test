[access]
	inheritFrom = All-Projects
[access "refs/*"]
	owner = group user/codedoe (codedoe)
[access "refs/for/refs/*"]
	read = group user/codedoe (codedoe)
	push = group user/codedoe (codedoe)
	label-Code-Review = -2..+2 group user/codedoe (codedoe)
	label-Verified = -1..+1 group user/codedoe (codedoe)
	rebase = group user/codedoe (codedoe)
	removeReviewer = group user/codedoe (codedoe)
	submit = group user/codedoe (codedoe)
[access "refs/heads/*"]
	read = group user/codedoe (codedoe)
	create = group user/codedoe (codedoe)
	push = +force group user/codedoe (codedoe)
	pushMerge = group user/codedoe (codedoe)
[access "refs/tags/*"]
	push = +force group user/codedoe (codedoe)
	pushTag = group user/codedoe (codedoe)
	pushSignedTag = group user/codedoe (codedoe)
